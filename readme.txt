To run this demo, all that is needed is a web browser. Opening the index.html file in the browser will run the demo.

It is using basic JavaScript functionality without any additional libraries to create an animated scene with Circles and Squares bouncing around a canvas;